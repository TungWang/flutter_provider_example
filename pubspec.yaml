name: flutter_provider_example
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:

  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  provider: ^4.3.2+2
  http: ^0.12.0+4
  modal_progress_hud: ^0.1.3
  transparent_image: ^1.0.0
  shared_preferences: ^0.5.6+3

dev_dependencies:

  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
